server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router client-side routing — serve index.html for any path that
    # isn't a real static asset, instead of nginx's default 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
